# Seed prompt contains internal scheduling parameters; keep it local only.
docs/seed-prompt.md

# Local drafts: only finalized documents get committed (git history is public and permanent).
docs/**/drafts/

# Site build tooling
node_modules/

# Built site assets (generated by npm run build / CI)
assets/site.css
assets/fonts/*.woff2
